export const LoginStatus = Object.freeze({
    REQUESTING: 0,
    LOGGED_IN: 1,
    LOGGED_OUT: 2
});

export const RequestStatus = Object.freeze({
    REQUESTING: 0,
    REQUESTED: 1,
    RESOLVED: 2
});
